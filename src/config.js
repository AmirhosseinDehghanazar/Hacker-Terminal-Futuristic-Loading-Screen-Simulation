const config = {
  themeColorDefault: "#00FF00", // Default theme color (green)
  themeColor50: "#FFA500", // Color when progress reaches 50% (orange)
  themeColor80: "#9cbcf0", // Color when progress reaches 80% (light blue)
  bgColor: "#000000", // Background color (black)
  typingSpeed: 2, // Speed of typing animation in milliseconds
  glitchProbability: 0.2, // Probability of glitch occurrence
  codeScrollSpeed: 50, // Speed of code scrolling effect in milliseconds
  matrixEffectSpeed: 33, // Speed of matrix effect animation in milliseconds
  realLoading: true, // Use real loading or fallback to randomNumber
  randomNumber: Math.random(), // Fallback random number for success/failure
};
